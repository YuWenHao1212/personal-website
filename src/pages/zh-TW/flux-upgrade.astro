---
import BaseLayout from '../../layouts/BaseLayout.astro';

const title = 'FLUX Vault v1 → v2 升級指南';
const description = 'FLUX-standard vault 升級指南：下載升級包，讓 Claude Code 自動完成 v1 → v2 升級。';
const build = '2026-08-08.1';
---

<BaseLayout title={title} description={description} hideLanguageSwitcher={true} noindex={true}>
  <article class="py-10 md:py-14 lg:py-16">
    <div class="container-prose">
      <header class="mb-8">
        <h1 class="text-3xl md:text-4xl font-bold mb-3">FLUX Vault v1 → v2 升級</h1>
        <p class="text-lg text-ink-600">給已經在用 v1 的你 — 三步、幾分鐘、Claude 自己跑</p>
        <p class="text-sm text-ink-400 mt-2">余文皓 | mail@yu-wenhao.com | 目前 build：{build}</p>
      </header>

      <div class="prose prose-lg max-w-none [&_blockquote]:!border-l-slate-400">
        <blockquote>
          <p>
            <strong>你的資料一個字都不會動</strong> — 升級前 Claude 會先做 git 備份，
            過程中會問你 2–3 個確認，照實回答就行。<br />
            升級完成後<strong>日常用法完全不變</strong>。
          </p>
        </blockquote>

        <h2>v2 新增了什麼？</h2>

        <ul>
          <li><strong>任務看板選配模組</strong>（Tracking Board）— 沉睡出貨，不啟用就完全沒感覺；哪天想要一個手機也能看的任務看板，三步啟用</li>
          <li><strong>一批文件修正</strong> — Guides、Templates、每日流程的小修</li>
        </ul>

        <hr />

        <h2>一、下載升級包</h2>

        <p>
          <a href="/downloads/flux-v2-upgrade.zip" download class="inline-block px-6 py-3 bg-accent text-white rounded-lg font-bold hover:bg-accent/90 transition-colors no-underline">
            📥 下載 flux-v2-upgrade.zip
          </a>
        </p>

        <hr />

        <h2>二、解壓、放到桌面</h2>

        <ol>
          <li>雙擊解壓下載的 <code>flux-v2-upgrade.zip</code>（Windows：右鍵 → 解壓縮全部）</li>
          <li>把解壓出來的 <code>flux-v2-upgrade</code> 資料夾<strong>放到桌面</strong></li>
        </ol>

        <blockquote>
          <p>⚠️ 放桌面就好，<strong>不要放進 FLUX Vault 資料夾裡面</strong>。</p>
        </blockquote>

        <hr />

        <h2>三、貼一句話給 Claude</h2>

        <ol>
          <li>先<strong>關掉 Obsidian</strong>（有開的話）</li>
          <li>用 <strong>VS Code</strong> 打開你的 <code>FLUX Vault</code> 資料夾（跟平常一樣）</li>
          <li>按 <code>Ctrl + `</code> 打開終端機、輸入 <code>claude</code> 啟動</li>
          <li>貼上這句：</li>
        </ol>

        <pre><code>請讀桌面的 flux-v2-upgrade/UPGRADE.md 照著做</code></pre>

        <p>
          接下來 Claude 會自己跑：備份 → 換新檔案 → 裝入看板模組 → 逐項驗證 → 給你一張改動總表。
          過程中它會問你幾個確認（例如「這段你自己加的內容要保留嗎？」）— 照實回答即可。
        </p>

        <p><strong>✓ 看到 Claude 回報「升級完成」+ 改動總表 = 完成。</strong></p>

        <hr />

        <h2>常見狀況</h2>

        <ul>
          <li><strong>升級中途斷掉了？</strong> 重新貼同一句話就好 — Claude 會偵測到上次跑到一半，從缺的部分接著補，不會重複破壞。</li>
          <li><strong>不確定自己是不是已經升過？</strong> 直接跑一次也沒關係 — 已是最新版會直接告訴你「不用升級」；裝到舊版 v2 的話會自動補到最新。</li>
          <li><strong>自己改過 vault 裡的檔案？</strong> Claude 會逐檔問你要保留還是換新，你的改動不會被無聲蓋掉；就算選錯，git 備份（<code>pre-v2</code>）也找得回來。</li>
        </ul>

        <hr />

        <h2>卡住的時候</h2>

        <ol>
          <li>把畫面截圖</li>
          <li>傳給我（LINE 或 email <a href="mailto:mail@yu-wenhao.com">mail@yu-wenhao.com</a>）</li>
        </ol>

        <p>我會盡快回覆協助。</p>

        <p><em>余文皓 | mail@yu-wenhao.com</em></p>
      </div>
    </div>
  </article>
</BaseLayout>
