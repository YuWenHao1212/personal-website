---
import BaseLayout from '../../../layouts/BaseLayout.astro';

const title = 'STUDIO A 學員專屬 — 三日工作坊課前安裝指南';
const description = '6/2 / 6/5 / 6/9 三日工作坊課前環境建置指南：安裝 Claude Pro、VS Code、Claude Code、Obsidian、Claude for Excel，下載 FLUX-standard-studio-a vault。';
---

<BaseLayout title={title} description={description} hideLanguageSwitcher={true} noindex={true}>
  <article class="py-10 md:py-14 lg:py-16">
    <div class="container-prose">
      <header class="mb-8">
        <h1 class="text-3xl md:text-4xl font-bold mb-3">STUDIO A 學員專屬安裝指南</h1>
        <p class="text-lg text-ink-600">三日工作坊（6/2、6/5、6/9）課前環境建置</p>
        <p class="text-sm text-ink-400 mt-2">講師：余文皓 | mail@yu-wenhao.com</p>
      </header>

      <div class="prose prose-lg max-w-none [&_blockquote]:!border-l-slate-400">
        <p>這份指南專為 STUDIO A 員工的 Mac 電腦設計。整份要做的事分成兩個階段：</p>

        <div class="not-prose my-8 grid grid-cols-1 md:grid-cols-[1fr_auto_1fr] gap-4 md:gap-3 items-stretch">
          <div class="flex flex-col p-5 rounded-lg border-2 border-accent/40 bg-accent/5">
            <div class="text-xs font-semibold text-accent uppercase tracking-wider mb-2">階段 1</div>
            <div class="text-lg font-bold mb-3">💻 你自己做</div>
            <ul class="text-sm space-y-1.5 text-ink-700 flex-1 list-none pl-0">
              <li>• 確認 Claude Pro 帳號</li>
              <li>• 裝 4 個工具<br /><span class="text-xs text-ink-500 pl-3">VS Code / Claude Code / Obsidian / Git</span></li>
              <li>• 下載 + 解壓 vault 到桌面</li>
            </ul>
            <div class="text-xs text-ink-500 mt-3 pt-3 border-t border-ink-200">⏱ 約 20 分鐘</div>
          </div>

          <div class="flex items-center justify-center text-2xl text-ink-400 md:rotate-0 rotate-90">→</div>

          <div class="flex flex-col p-5 rounded-lg border-2 border-accent/40 bg-accent/5">
            <div class="text-xs font-semibold text-accent uppercase tracking-wider mb-2">階段 2</div>
            <div class="text-lg font-bold mb-3">🤖 Claude 自己跑</div>
            <ul class="text-sm space-y-1.5 text-ink-700 flex-1 list-none pl-0">
              <li>• 確認行事曆 / 郵件 app 平常能用</li>
              <li>• Claude 讀 setup script、自動安裝 email + calendar skill</li>
              <li>• 驗證安裝成功</li>
            </ul>
            <div class="text-xs text-ink-500 mt-3 pt-3 border-t border-ink-200">⏱ 約 10-20 分鐘</div>
          </div>
        </div>

        <p>💡 另外有一個 <strong>Bonus 章節 — Claude for Excel</strong>。這不是 Claude Code、是 Anthropic 在 Excel 的另一個整合，<strong>工作坊會用到、請務必裝完</strong>。</p>

        <hr />

        <div class="not-prose mt-16 mb-8 py-6 px-6 bg-accent text-white rounded-lg">
          <div class="text-xs font-semibold uppercase tracking-wider mb-1 opacity-80">階段 1 / 2</div>
          <div class="text-2xl font-bold">💻 你自己做（步驟一 ~ 六）</div>
          <div class="text-sm mt-1 opacity-80">確認帳號 + 裝 4 個工具 + 下載 vault · 約 20 分鐘</div>
        </div>

        <div class="border-l-4 border-accent pl-6 my-6">

        <h2>一、確認 Claude Pro 帳號（已有帳號可跳過）</h2>

        <ol>
          <li>前往 <a href="https://claude.ai" target="_blank" rel="noopener">claude.ai</a> 登入</li>
          <li>確認左下角顯示 <strong>Pro</strong> 字樣（沒有 Pro 訂閱、後面 Claude Code 跑不起來）</li>
        </ol>

        <img src="/images/workshop/setup/03-claude-pro-badge.png" alt="claude.ai 左下角顯示 Pro 字樣 — 確認訂閱已生效" />

        <blockquote>
          <p>圖中是年繳方案（$17/月）— 月繳（$20/月）也 OK，只要有 <strong>Pro</strong> 訂閱即可。</p>
        </blockquote>

        <hr />

        <h2>二、安裝 VS Code（編輯器）</h2>

        <p>VS Code 是看 <code>.md</code> 檔、改設定檔的工具。</p>

        <ol>
          <li>前往 <a href="https://code.visualstudio.com/Download" target="_blank" rel="noopener">VS Code 下載頁面</a></li>
          <li>下載 Mac 版</li>
          <li>打開 <code>.zip</code> 檔案，將 <strong>Visual Studio Code.app</strong> 拖進<strong>應用程式</strong>資料夾</li>
          <li>打開 VS Code，確認可以正常啟動</li>
        </ol>

        <h3>安裝 VS Code 擴充功能</h3>

        <ol>
          <li>打開 VS Code</li>
          <li>按 <code>Cmd + Shift + X</code> 打開擴充功能側欄</li>
          <li>搜尋並安裝以下兩個擴充功能：</li>
        </ol>

        <p><strong>① Claude Code</strong>（作者 Anthropic）— 讓 VS Code 跟 Claude 整合，提供兩種對話方式：(a) 在 VS Code 內建 terminal 跑 <code>claude</code> 指令（工作坊主要用這個、搭配 Section 四 裝的 CLI）(b) 按 <code>Cmd + Esc</code> 喚出獨立對話面板。<strong>一定要裝</strong>。</p>

        <p>搜「<code>claude code</code>」，選作者是 <strong>Anthropic</strong> 那個（識別碼 <code>anthropic.claude-code</code>）：</p>

        <img src="/images/studio-a-install/01-claude-code-extension.webp" alt="VS Code 擴充功能市集 — Claude Code for VS Code（Anthropic 出品）" />

        <p><strong>② Chinese (Traditional) Language Pack</strong>（作者 Microsoft）— VS Code 介面中文化、選用。</p>

        <p>搜「<code>Chinese</code>」，選作者是 <strong>Microsoft</strong>、寫「<strong>中文(繁體)</strong>」那個：</p>

        <img src="/images/studio-a-install/02-chinese-language-pack.webp" alt="VS Code 擴充功能市集 — Chinese (Traditional) Language Pack（Microsoft 出品）" />

        <ol start={4}>
          <li>兩個都裝完、重新啟動 VS Code</li>
          <li>裝好 Claude Code extension 後，VS Code 右上角會出現一個 <strong>✱</strong> 符號 — 點它可以開 Claude 對話面板（第一次會要求登入 Anthropic 帳號，等下面第七步「讓 Claude 跑 setup」一起處理就好）</li>
        </ol>

        <hr />

        <h2>三、安裝 Git（透過 macOS Command Line Tools）</h2>

        <p>Git 是版本控制工具、後續 Claude workflow 會用到。同時這個安裝會把 <strong>Python 3</strong> 也一起裝上（後續 email skill 需要）。macOS 沒預裝、需透過終端機觸發 macOS Command Line Tools 安裝：</p>

        <ol>
          <li>打開剛裝好的 <strong>VS Code</strong>（不用開資料夾、空狀態即可）</li>
          <li>按 <code>Cmd + J</code> 打開 VS Code 內建終端機（cwd 在你的家目錄）</li>
          <li>貼上以下指令、按 Enter：</li>
        </ol>

        <pre><code>git --version</code></pre>

        <p>第一次跑會跳「安裝命令列工具」對話框（macOS 內建的開發者工具包、約 700MB、含 Git + Python + 其他）— 點<strong>安裝</strong>。下載 + 安裝完成後再跑一次 <code>git --version</code>、看到版本號（例 <code>git version 2.39.x</code>）= 成功。</p>

        <hr />

        <h2>四、安裝 Claude Code CLI 並登入</h2>

        <p>Claude Code 是讓 AI 直接在你電腦裡讀寫檔案、執行指令的工具 — 工作坊全程主要用這個 CLI 版本（Section 二 裝的 extension 是 GUI 版本、互補不衝突）。</p>

        <ol>
          <li>繼續用剛才的 VS Code 內建終端機（沒開的話按 <code>Cmd + J</code> 打開）</li>
          <li>貼上以下指令、按 Enter 執行：</li>
        </ol>

        <pre><code>curl -fsSL https://claude.ai/install.sh | bash</code></pre>

        <ol start={3}>
          <li>安裝完成後，貼上以下指令設定路徑：</li>
        </ol>

        <pre><code>{`echo 'export PATH="$HOME/.local/bin:$PATH"' >> ~/.zshrc
source ~/.zshrc`}</code></pre>

        <ol start={4}>
          <li>關閉這個終端機（輸入 <code>exit</code>）、再按 <code>Cmd + J</code> 打開新的</li>
          <li>輸入 <code>claude --version</code> 驗證有版本號 = 裝好了</li>
        </ol>

        <blockquote>
          <p>暫時不要跑 <code>claude</code>、登入留到 Section 七（讓 Claude 跑 setup）時做 — 那時 vault 已下載、<code>claude</code> 跑起來會自動讀 vault 的 <code>CLAUDE.md</code>、體驗最完整。</p>
        </blockquote>

        <hr />

        <h2>五、下載 FLUX-standard-studio-a vault 並用 VS Code 打開</h2>

        <p>FLUX-standard-studio-a 是為 STUDIO A 員工工作場景設計的個人 AI 工作區。</p>

        <ol>
          <li>依你的作業系統點對應連結下載 vault：</li>
        </ol>

        <p class="flex gap-3 flex-wrap">
          <span>
            <a href="/downloads/FLUX-standard-studio-a-mac.zip" download class="inline-block px-6 py-3 bg-accent text-white rounded-lg font-bold hover:bg-accent/90 transition-colors no-underline">
              📥 下載 FLUX-standard-studio-a-mac.zip
            </a>
          </span>
          <span>
            <a href="/downloads/FLUX-standard-studio-a-windows.zip" download class="inline-block px-6 py-3 bg-accent text-white rounded-lg font-bold hover:bg-accent/90 transition-colors no-underline">
              📥 下載 FLUX-standard-studio-a-windows.zip
            </a>
          </span>
        </p>

        <blockquote>
          <p>Windows 用 windows.zip — 解壓後資料夾叫 <code>_claude</code> / <code>_obsidian</code>（Windows 預設會藏 <code>.</code> 開頭資料夾、改名讓你看得到）。後面步驟七 Claude 會自動幫你改回 <code>.claude</code> / <code>.obsidian</code>、不用手動處理。</p>
        </blockquote>

        <ol start={2}>
          <li>下載後雙擊 <code>.zip</code> 解壓縮</li>
          <li>把解壓縮後的 <code>FLUX-standard-studio-a</code> 資料夾<strong>放到桌面</strong>（最容易找）</li>
        </ol>

        <ol start={4}>
          <li>打開 <strong>VS Code</strong>，選單 <strong>File → Open Folder…</strong>（檔案 → 開啟資料夾），選剛才那個 <code>FLUX-standard-studio-a</code> 資料夾</li>
          <li>VS Code 會問是否信任這個資料夾的作者 — 點<strong>「是、信任」</strong>（Yes, I trust the authors）</li>
          <li>左側 Explorer 看到 <code>Inbox / Atlas / Calendar / Cockpit / Efforts / Guides / Templates / CLAUDE.md / Home.md / sortspec.md</code> = 開對了（<code>sortspec.md</code> 是 Obsidian 排序用、不用動）</li>
        </ol>

        <hr />

        <h2>六、安裝 Obsidian 並打開 FLUX vault</h2>

        <p>Obsidian 是個人筆記庫，所有任務、日記、知識卡片都在這裡看（之後上課會用 Obsidian 看筆記、用 VS Code 跟 Claude 對話）。</p>

        <ol>
          <li>前往 <a href="https://obsidian.md/download" target="_blank" rel="noopener">Obsidian 下載頁面</a></li>
          <li>下載 Mac 版</li>
          <li>打開下載的 <code>.dmg</code> 檔案，將 <strong>Obsidian</strong> 拖進<strong>應用程式</strong>資料夾</li>
          <li>打開 <strong>Obsidian</strong>，選單列點 <strong>File → Open Vault...</strong>（檔案 → 開啟保管庫）</li>
          <li>跳出 Vault Switcher 視窗、點 <strong>Open folder as vault</strong>（開啟資料夾作為保管庫）</li>
          <li>選剛才解壓的 <code>FLUX-standard-studio-a</code> 資料夾</li>
          <li>Obsidian 問是否信任此 Vault 的外掛 — 點<strong>信任並啟用</strong></li>
        </ol>

        </div>

        <hr />

        <div class="not-prose mt-16 mb-8 py-6 px-6 bg-emerald-700 text-white rounded-lg">
          <div class="text-xs font-semibold uppercase tracking-wider mb-1 opacity-80">階段 2 / 2</div>
          <div class="text-2xl font-bold">🤖 Claude 自己跑（步驟七 ~ 八）</div>
          <div class="text-sm mt-1 opacity-80">Claude 自動安裝 email + calendar skill + 你驗證 · 約 10-20 分鐘</div>
        </div>

        <div class="border-l-4 border-emerald-500 pl-6 my-6">

        <h2>七、讓 Claude 跑 setup</h2>

        <blockquote>
          <p><strong>跑 setup 前確認 Mac 兩個內建 app 平常都能用</strong>：</p>
          <ul>
            <li>✅ <strong>行事曆 app</strong> — 打開能看到自己的行程</li>
            <li>✅ <strong>郵件 app</strong> — 能正常收發 STUDIO A 公司信</li>
          </ul>
          <p>沒設好的話 skill 跑不起來、依平常方式找 STUDIO A IT 處理。</p>
        </blockquote>

        <p>承接上一步、在 VS Code 已開啟 <code>FLUX-standard-studio-a</code> 資料夾的視窗、按 <code>Cmd + J</code> 打開 VS Code 內建終端機（cwd 自動在 vault），輸入：</p>

        <pre><code>claude</code></pre>

        <ol>
          <li>第一次跑會要求登入 — 按畫面指示登入你的 Claude 帳號（同 claude.ai 那個）</li>
          <li>登入後 Claude 自動讀 vault 內的 <code>CLAUDE.md</code>、知道這是你的個人 vault</li>
          <li>跟 Claude 說：</li>
        </ol>

        <pre><code>請讀取 Inbox/SETUP-CLAUDE.md 並依步驟執行</code></pre>

        <p>Claude 會：偵測 vault 結構 → Windows 解壓的話自動把 <code>_claude</code> / <code>_obsidian</code> rename 回 <code>.claude</code> / <code>.obsidian</code>（Mac 解壓的話這步自動跳過）→ 驗證 vault 完整 → 自動刪除本檔。過程中可能跳出「允許執行」的彈窗、按「允許」即可。</p>

        <p>完成後 Claude 會提示你下一步。接著跟 Claude 說：</p>

        <pre><code>請依 Inbox/setup-claude-skills.md 安裝 Claude skill</code></pre>

        <p>Claude 會自動讀 <code>Inbox/setup-claude-skills.md</code>、依序跑：</p>

        <ul>
          <li><strong>Part A：Email skill</strong> — 設定 Claude 能讀寫你的公司信件。過程中會跳 macOS <strong>鑰匙圈授權</strong>對話框、按<strong>「永遠允許」</strong>+ 輸入 Mac 登入密碼即可。<strong>測試</strong>：Claude 讀最近幾封信給你看 + 在 Mail 存 2 個測試草稿（純文字 + HTML）→ 你打開 Mail 草稿匣確認兩封都在。</li>
          <li><strong>Part B：Calendar skill</strong> — 問你工作行事曆名稱（在 Apple Calendar 左側欄看到的那個）。<strong>測試</strong>：Claude 讀未來 7 天行程給你看。</li>
          <li><strong>Part C：結案</strong> — Claude 印出安裝結果摘要（email 帳號代號 + 日曆名稱）+ 給你 3 句馬上能試的測試指令（讀信摘要 / 看今天會議 / 寄一封信）。</li>
        </ul>

        <blockquote>
          <p><strong>⚠️ 過程中 macOS 會跳兩次系統彈窗</strong>：「Terminal 想要控制 Mail」「Terminal 想要控制 Calendar」 — 請按「<strong>好</strong>」/「<strong>允許</strong>」、Claude 才能繼續。</p>
        </blockquote>

        <blockquote>
          <p><strong>裝到一半卡住？</strong>Claude 會跟你說「請找講師」就停下來。不要自己 debug 公司 mail server 設定 — 聯繫 STUDIO A 工作坊承辦窗口、或直接寫信給講師 <a href="mailto:mail@yu-wenhao.com">mail@yu-wenhao.com</a>，4 小時內會有回覆。</p>
        </blockquote>

        <hr />

        <h2>八、驗證安裝成功</h2>

        <p>跟 Claude 說：「<strong>跑驗證</strong>」、或自己手動試這兩件事：</p>

        <ol>
          <li>跟 Claude 說：「<strong>讀我今天最後 5 封信、做摘要</strong>」 — 看到 Claude 列出 5 封信的主旨 + 摘要 = email skill OK</li>
          <li>跟 Claude 說：「<strong>看我今天的會議</strong>」 — 看到今天行程列表 = calendar skill OK</li>
        </ol>

        <p>下方逐項打勾：</p>

        <ul class="list-none pl-0">
          <li class="flex items-start gap-2"><input type="checkbox" class="mt-1.5 accent-accent" /> Claude Pro 帳號可登入 claude.ai</li>
          <li class="flex items-start gap-2"><input type="checkbox" class="mt-1.5 accent-accent" /> VS Code 安裝完成、可正常開啟</li>
          <li class="flex items-start gap-2"><input type="checkbox" class="mt-1.5 accent-accent" /> 終端機 <code>git --version</code> 有輸出</li>
          <li class="flex items-start gap-2"><input type="checkbox" class="mt-1.5 accent-accent" /> 終端機 <code>claude</code> 能進入 Claude Code</li>
          <li class="flex items-start gap-2"><input type="checkbox" class="mt-1.5 accent-accent" /> Obsidian 可開啟 FLUX-standard-studio-a vault</li>
          <li class="flex items-start gap-2"><input type="checkbox" class="mt-1.5 accent-accent" /> Apple Mail 能收發 STUDIO A 公司信</li>
          <li class="flex items-start gap-2"><input type="checkbox" class="mt-1.5 accent-accent" /> Apple Calendar 看得到工作行程</li>
          <li class="flex items-start gap-2"><input type="checkbox" class="mt-1.5 accent-accent" /> Claude 能讀公司 email、做摘要</li>
          <li class="flex items-start gap-2"><input type="checkbox" class="mt-1.5 accent-accent" /> Claude 能看公司行事曆</li>
          <li class="flex items-start gap-2"><input type="checkbox" class="mt-1.5 accent-accent" /> Excel Web 能開啟 Claude sidebar、能回答試算表內容（Bonus）</li>
        </ul>

        <p>都打勾，你就準備好上 6/2 第一堂了！</p>

        </div>

        <hr />

        <div class="not-prose mt-16 mb-8 py-6 px-6 bg-amber-600 text-white rounded-lg">
          <div class="text-xs font-semibold uppercase tracking-wider mb-1 opacity-80">BONUS / 加分題</div>
          <div class="text-2xl font-bold">🎁 Claude for Excel（必裝）</div>
          <div class="text-sm mt-1 opacity-80">不是 Claude Code、是 Anthropic 在 Excel 的另一個整合 · ⏱ 約 10 分鐘</div>
        </div>

        <div class="border-l-4 border-amber-500 pl-6 my-6">

        <h2>九、安裝 Claude for Excel（Web 版）</h2>

        <p>Claude for Excel 是 Anthropic 官方的 Excel add-in、讓你在試算表裡直接用 Claude 分析資料、改公式、跑 audit。</p>

        <p><strong>前置條件</strong>：</p>

        <ul>
          <li>✅ Microsoft 365 訂閱（STUDIO A 給你的公司帳號）</li>
          <li>✅ Claude Pro 訂閱（步驟一已確認）</li>
        </ul>

        <p><strong>步驟</strong>：</p>

        <ol>
          <li>前往 <a href="https://office.com" target="_blank" rel="noopener">office.com</a>、用 STUDIO A 給你的 Microsoft 365 公司帳號登入</li>
          <li>點左側 Excel icon、開啟 Excel Web 版、開一個空白活頁簿</li>
          <li>直接前往 Microsoft Marketplace：<a href="https://appsource.microsoft.com/en-us/product/office/WA200009404" target="_blank" rel="noopener">Claude by Anthropic for Excel</a></li>
          <li>點藍色「<strong>Get it now</strong>」/「<strong>立即取得</strong>」按鈕</li>
        </ol>

        <blockquote>
          <p>頁面會顯示「<strong>Additional purchase may be required</strong>（可能需要額外購買）」— 這是 Microsoft 的標準提示。<strong>裝 add-in 本身免費</strong>、Microsoft 不會跟你收錢；要使用 Claude 服務需要 Claude Pro 訂閱（你已經有了）。</p>
        </blockquote>

        <img src="/images/studio-a-install/03-claude-excel-appsource.webp" alt="Microsoft Marketplace 的 Claude by Anthropic for Excel 頁面 — 點藍色 Get it now 按鈕安裝" />

        <ol start={5}>
          <li>跟著 Microsoft 提示完成授權、回到 Excel Web</li>
          <li>裝好後 <strong>Claude 按鈕會直接 pin 在 Excel ribbon 上</strong>（Home 區塊右側、橘色 ✱ 圖示）— 點它打開右側 sidebar</li>
          <li>Sidebar 點「<strong>Sign in</strong>」、用你的 Claude 帳號（同 claude.ai）登入並授權</li>
          <li><strong>驗證</strong>：在 A1 輸入 <code>Hello</code>、在 sidebar 問「這個試算表有什麼？」、Claude 能回答 = 成功</li>
        </ol>

        <img src="/images/studio-a-install/04-claude-excel-sidebar.webp" alt="Excel Web 開啟 Claude sidebar — ribbon 右側有 Claude 按鈕、右側 sidebar 顯示對話介面" />

        </div>

        <hr />

        <h2>卡住的時候</h2>

        <ol>
          <li><strong>Mail / Calendar app 不能用</strong>（收不到信、看不到行事曆）→ 依平常方式找 STUDIO A IT</li>
          <li><strong>其他任何卡住</strong>（環境裝不起來 / Claude 跑 setup 卡住 / 不知道怎麼辦）→ 聯繫 STUDIO A 工作坊承辦窗口、或直接寫信給講師 <a href="mailto:mail@yu-wenhao.com">mail@yu-wenhao.com</a>，4 小時內回覆。</li>
        </ol>

        <p><em>余文皓 | mail@yu-wenhao.com</em></p>
      </div>
    </div>
  </article>
</BaseLayout>
