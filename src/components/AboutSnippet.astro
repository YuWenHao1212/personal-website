---
import { getLangFromUrl, useTranslations } from '../i18n/utils';

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);

const content = {
  'zh-TW': {
    text: '我是 WenHao，我用 AI 打造產品。在這個時代，個人可以創造出以前需要整個團隊才能做到的東西。',
    cta: '閱讀完整故事',
  },
  'en': {
    text: "I'm WenHao, and I build products with AI. In this era, individuals can create what previously required entire teams.",
    cta: 'Read My Full Story',
  },
};
---

<section class="py-10 md:py-14 lg:py-16">
  <div class="container-narrow">
    <h2 class="text-xl md:text-2xl lg:text-3xl font-bold text-ink-900 mb-4 md:mb-6 text-center">
      {t('section.about')}
    </h2>

    <div class="bg-cream-50 rounded-2xl p-6 md:p-8 text-center">
      <p class="text-base md:text-lg text-ink-700 mb-4 md:mb-6 max-w-2xl mx-auto">
        {content[lang].text}
      </p>
      <a href={`/${lang}/about`} class="btn-outline text-sm md:text-base">
        {content[lang].cta}
      </a>
    </div>
  </div>
</section>
